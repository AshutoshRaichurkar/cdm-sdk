# cdm-sdk


![Screenshot of a comment on a GitHub issue showing an image, added in the Markdown, of an Octocat smiling and raising a tentacle.](https://stock.adobe.com/search?k=smiling+dog+funny)


## How to start this project
```
1. Creating an python virtual environment:
    python3 -m venv .venv

2. Activating the virtual environment
    source .venv/bin/activate

3. Install Java (Spark needs it)
    brew install openjdk@17
    echo 'export PATH="$(brew --prefix openjdk@17)/bin:$PATH"' >> ~/.zshrc
    source ~/.zshrc
    java -version

4. Install PySpark
    python -m pip install --upgrade pip
    pip install pyspark
```

