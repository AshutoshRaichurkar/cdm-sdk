# cdm-sdk

How to build the wheel file locally

1. cd C:\Ashutosh\Boston\cdm-sdk

2. python -m venv .venv
3. .\.venv\Scripts\Activate.ps1

4. python -m pip install --upgrade pip
5. python -m pip install build

6. python -m build


