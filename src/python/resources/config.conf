app {
  name = "orders-service"
  env = "dev"          # comments allowed
  port = 8080
  debug = true

  db {
    host = "localhost"
    port = 5432
    user = "postgres"
    # password = ${?DB_PASSWORD}   # read from env var if set, else null
  }

  features = {
    new_ui = true
    retries = 3
  }

  # arrays
  allowed_countries = ["US", "CA", "IN"]
}
